# Measurement Device Manager 正式工作计划

更新日期：2026-06-02
适用目录：Z:\Measurement_Device_Manager\matlab_unified
主线 GUI：MeasurementManagerApp.m / 后续版本文件
参考旧代码：旧版 MainGui.m、elog_parser.m、elog_write_function.m 仅用于参考 ELOG 实现方式，不作为当前主线 GUI 开发对象。

## 一、工作边界

1. 当前主线是统一 MATLAB 仪器控制 APP。
2. 老版 MainGui.m 只作为历史参考，不继续作为主 GUI 修改。
3. 没有用户明确授权时，不连接真实仪器，不触发采集、扫描、digitize 或实时数据保存。
4. 默认优先只读当前显示数据；只有用户明确要求主动采集时，才使用会触发仪器动作的模式。
5. 大版本更新必须先做版本记录，再开发；开发完成后必须提交新版本到 Git。

## 二、子任务一：仪器参数设置

目标：连接设备后读取当前仪器参数，显示到 GUI 可编辑参数面板；用户修改后点击 Set 按钮写回仪器。

OSA 参数：
- 波长范围
- 分辨率
- 采样间隔
- 能级

ESA 参数：
- 频率范围

OSC 参数：
- 各输入通道独立电压垂直量程
- 时间窗口量程
- 采集通道数
- 采样率

验收标准：
- 连接后能自动读取当前参数并显示。
- 参数输入框可编辑。
- Set 按钮只修改用户明确提交的参数。
- 未连接设备时 Set 给出明确提示，不崩溃。

## 三、子任务二：采集模式添加和验证

目标：不同仪器按实际使用方式提供明确采集模式，并避免误触发。

OSA 模式：
- 读取当前屏幕暂停数据；如正在连续扫描，先暂停再保存。
- 单次扫描后保存。
- 重复扫描并保存，可设置扫描间隔。

ESA 模式：
- 读取当前屏幕暂停数据；如正在连续扫描，先暂停再保存。
- 单次扫描后保存。
- 重复扫描并保存，可设置扫描间隔。

OSC 模式：
- 直接读取当前数据。
- 数据采集器模式：采集指定通道在指定时间内的电压变化，可设置采集时间和采样率。采集时间需要根据采集深度换算，可参考 Z:\Measurement_Device_Manager\RigolDHO4000.m。

验收标准：
- GUI 中每种仪器只显示适用模式。
- 默认模式不主动触发新扫描。
- 主动扫描类模式必须由用户明确选择。
- 重复采集支持停止或中断，不锁死 GUI。

## 四、子任务三：GUI 交互优化

目标：让界面适合日常实验使用，优先解决可见性、布局、误操作和数据管理问题。

计划内容：
- Live Data View 表格支持单条数据右键删除。
- 不同仪器类型使用不同颜色标注。
- 绘图曲线按仪器和通道使用不同颜色，OSC 多通道不得颜色重合。
- 数据保存支持两种分类方式：按仪器分类、按使用人分类。
- 小窗口下 UI 不能裁切，可采用面板内滚动区域。
- ELOG 配置后续再更新，当前仅保留可见、可用、可验证的上传入口。
- 界面整体改紧凑，尤其优化 connect / disconnect / acquire 按钮布局。
- 后续增加定时采集、按次数采集。
- 示波器时间轴改成相对时间差，并根据时间量程自动调整单位。
- 增加 FIG 格式保存选项。

验收标准：
- 主要按钮在 1400x850 或更小窗口下不被裁切。
- ELOG 上传按钮、数据保存按钮、状态反馈始终可见。
- 小窗口使用时可通过滚动访问全部控件。
- 删除数据只影响所选记录，不影响已保存文件。

## 五、ELOG 工作要求

当前要求：
- ELOG 功能接入统一 APP。
- 上传按钮必须在 GUI 中可见。
- 上传结果必须在 GUI 状态区和本地日志中可验证。
- ELOG 主机默认使用 192.168.1.72。
- 本机 ELOG 可执行文件优先使用 E:\Program Files (x86)\ELOG\elog.exe。

暂缓内容：
- ELOG 具体字段和实验室 logbook 配置等用户确认后再改。

## 六、Git 版本控制流程

Git 仓库位置：Z:\Measurement_Device_Manager\matlab_unified\.git
当前远端：origin = https://github.com/OUTERI/Measurement-Device-Manager-matlab-unified.git
当前主分支：master

### 1. 每次开始大版本更新前

先检查仓库状态：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" status --short --branch
```

如果工作区有未提交的主线代码改动，先和用户确认这些改动属于谁、是否需要提交。

工具临时文件默认不提交，例如：
- .codegraph/daemon.pid
- .claude/

### 2. 保存旧版本

在开始大版本修改前，对当前可运行状态打 tag：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" tag -a vYYYYMMDD-before-<task> -m "Before <task>"
git -C "Z:\Measurement_Device_Manager\matlab_unified" push origin vYYYYMMDD-before-<task>
```

用途：
- 记录旧版本。
- 出问题时可回退查看。
- 不需要复制大量文件来保存旧版。

### 3. 开发新版本

开发过程中：
- 只修改与当前任务相关的文件。
- 不混入无关工具文件。
- 大改前优先创建 V2 / V3 文件或分支，等验证后再决定是否替换主版本。

可选分支流程：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" switch -c feature/<task-name>
```

如果用户不熟悉分支，默认仍可在 master 上开发，但每个阶段必须提交清楚。

### 4. 完成后提交新版本

检查变动：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" status --short
git -C "Z:\Measurement_Device_Manager\matlab_unified" diff --stat
```

只添加相关文件：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" add <相关文件>
```

提交：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" commit -m "feat: <简短说明>"
```

推送：
```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" push origin master
```

### 5. 给新版本打 tag

```powershell
git -C "Z:\Measurement_Device_Manager\matlab_unified" tag -a vYYYYMMDD-<task> -m "<task> completed"
git -C "Z:\Measurement_Device_Manager\matlab_unified" push origin vYYYYMMDD-<task>
```

### 6. 推荐提交信息格式

- feat: 新功能
- fix: bug 修复
- docs: 文档更新
- refactor: 不改变功能的结构调整
- ui: GUI 布局和交互调整
- test: 测试或验证脚本

示例：
```text
ui: improve v2 layout for storage and ELOG panels
feat: add OSA acquisition mode selector
fix: preserve current-display read mode without triggering scan
```

## 七、当前 Git 状态记录

读取时间：2026-06-02
当前分支：master
当前状态：master 跟踪 origin/master
最近提交：bfbdb59 fix: revert 2 broken fixes, apply correct alternatives

当前检测到的非主线变动：
- .codegraph/daemon.pid 删除
- .claude/ 未跟踪

处理建议：
- 这些工具文件不纳入 GUI 功能提交。
- 后续提交前需要确认是否加入 .gitignore 或恢复/清理。

## 八、近期建议优先级

1. 先稳定 GUI 布局，解决 Data Storage 和 ELOG Upload 面板裁切问题。
2. 确认 ELOG 上传入口可见、可点、可反馈。
3. 为当前布局稳定版提交 Git 版本并打 tag。
4. 再进入仪器参数读取和 Set 功能。
5. 最后扩展多采集模式和长期采集逻辑。
